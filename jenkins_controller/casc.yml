jenkins:
  securityRealm:
    local:
      users:
        - id: "panda"
          password: "panda"
  authorizationStrategy:
    globalMatrix:
      permissions:
        - "Overall/Administer:panda"
  numExecutors: 0
  credentials:
    usernamePassword:
      - username: admin
      - password: password
      - id: artifactory
      - scope: GLOBAL